{
    "example-moving-mesh-stress": {
        "title": "Moving Mesh Stress",
        "description": "900 individually-animated meshes with per-frame quaternion updates and world matrix recalculations.",
        "tags": ["stress-test", "animation"]
    },
    "example-static-mesh-stress": {
        "title": "Static Mesh Stress",
        "description": "900 static meshes with no per-frame updates — measures pure rendering overhead.",
        "tags": ["stress-test"]
    },
    "example-texture": {
        "title": "Texture",
        "description": "A textured spinning box demonstrating user-provided Texture, UV coordinates, and texture sampling.",
        "tags": ["texture"]
    },
    "example-mipmaps": {
        "title": "Mipmaps",
        "description": "Side-by-side comparison of textures with and without mipmaps. Left plane uses mipmaps + 16x anisotropic filtering, right plane has no mipmaps — notice the aliasing in the distance.",
        "tags": ["texture", "mipmaps"]
    },
    "example-instanced-mesh": {
        "title": "Instanced Mesh",
        "description": "30 instanced boxes with per-instance transforms and colours via InstancedBufferAttributeNode.",
        "tags": ["instancing"]
    },
    "example-instancing-storage-buffer": {
        "title": "Instancing with Storage Buffer",
        "description": "35 instanced boxes using storage buffers (mat4x4f transforms + vec4f colours) indexed by instance_index, combined with an instancedBufferAttribute for per-instance spin offset.",
        "tags": ["instancing", "storage-buffer"]
    },
    "example-compute-particles": {
        "title": "Compute Particles",
        "description": "8192 GPU-simulated particles updated each frame by a @compute shader (renderer.compute) and rendered as instanced quads — no CPU readback.",
        "tags": ["compute", "particles"]
    },
    "example-indirect-batched": {
        "title": "Indirect Batched",
        "description": "CPU-driven indirect rendering: one merged geometry holds box and sphere data; two IndirectBuffers select sub-ranges via firstIndex + instanceCount. A slider moves the box/sphere split at runtime.",
        "tags": ["indirect", "batched-mesh"]
    },
    "example-indirect-compute": {
        "title": "Indirect Compute",
        "description": "GPU-driven indirect rendering: a compute shader writes instanceCount directly into the indirect draw buffer each frame via IndirectBuffer.asStorageNode(), culling instances by distance — no CPU readback.",
        "tags": ["indirect", "compute", "culling"]
    },
    "example-mrt": {
        "title": "MRT (Multiple Render Targets)",
        "description": "Multiple Render Targets (MRT): renders a sphere to 4 targets (output, normal, diffuse, emissive) and composites them side-by-side for visualization.",
        "tags": ["mrt", "post-processing"]
    },
    "example-raging-sea": {
        "title": "Raging Sea",
        "description": "Animated ocean surface using sinusoidal large waves and multi-octave value noise small waves, with finite-difference normals and emissive wave crests — all computed on the GPU via vertex shader uniforms.",
        "tags": ["geometry", "animation", "noise"]
    },
    "example-uniforms": {
        "title": "Uniforms",
        "description": "Demonstrates two uniform patterns: inline uniforms (shared across meshes) and name-based uniforms (per-material values resolved at render time).",
        "tags": ["uniforms", "material"]
    },
    "example-storage": {
        "title": "Storage Buffers",
        "description": "Demonstrates two storage buffer patterns: inline storage (buffer passed directly) and name-based storage (resolved from geometry.buffers at render time).",
        "tags": ["storage-buffer", "instancing"]
    },
    "example-voxels": {
        "title": "Voxels",
        "description": "Procedurally generated voxel landscape using layered simplex noise, with culled meshing per chunk and per-vertex ambient occlusion computed on the CPU.",
        "tags": ["geometry", "noise", "voxels"]
    },
    "example-voxels-batched": {
        "title": "Voxels Batched",
        "description": "Batched voxel renderer with slab-allocated merged geometry, GPU frustum culling via compute shader, indirect draws, render distance management, and realtime voxel editing.",
        "tags": ["voxels", "indirect", "compute", "culling", "batched-mesh"]
    },
    "example-interleaved": {
        "title": "Interleaved Vertex Buffers",
        "description": "Demonstrates interleaved vertex attributes: normal and per-vertex color are packed into a single GpuBuffer with shared stride, producing one setVertexBuffer call for both attributes.",
        "tags": ["geometry", "interleaved", "vertex-buffers"]
    },
    "example-cubemap": {
        "title": "Cube Texture Skybox",
        "description": "Cube texture skybox rendered from inside a box. Each face has a distinct color and label to verify correct face mapping and cube texture sampling.",
        "tags": ["texture", "cubemap", "skybox"]
    },
    "example-discard": {
        "title": "Discard",
        "description": "Animated dissolve effect using the Discard() control flow node with a 4x4 Bayer dither matrix. Opacity oscillates via sin(time), discarding fragments below the dither threshold.",
        "tags": ["discard", "dithering", "control-flow"]
    },
    "example-shadow-map": {
        "title": "Shadow Map",
        "description": "Two-pass shadow mapping: a depth-only pass renders the scene from the light's orthographic camera into a depth texture, then the scene pass samples it with textureSampleCompare() to produce hard shadows.",
        "tags": ["shadows", "depth-texture", "render-target", "multi-pass"]
    }
}
